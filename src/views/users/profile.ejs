<%- include('../partials/head') %> 
<body>
    <header>
        <%- include('../partials/header')  %>
    </header>
    <main>
        <div class="container">
            <div class="row text-center">
                <h1>Perfil de Usuario</h1>
                <hr>
            <div>
                <img style="width: 350px;" src="/img/avatars/<%= locals.usuario.avatar  %>" alt="<%= locals.usuario.name %>">
                <p>Usuario: <%= locals.usuario.name %></p>
                <p>Email: <%= locals.usuario.email %></p>
            </div>
        </div>
    </main>
    <footer>
        <%- include('../partials/footer'); %>
    </footer>
</body>
</html>